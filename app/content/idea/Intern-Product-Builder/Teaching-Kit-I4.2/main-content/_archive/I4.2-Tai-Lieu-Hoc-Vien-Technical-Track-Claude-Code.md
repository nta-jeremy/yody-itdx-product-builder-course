# I4.2 — Tài Liệu Học: Xây Dựng AI Feature Có Trách Nhiệm
## Technical Track: Vibe Coding / Claude Code

> **Dành cho:** Học viên Yody Product Builder Program — Module 4 · L4 Integrator  
> **Thời lượng:** 180 phút (lý thuyết + thực hành)  
> **Phiên bản:** 1.0 · 25/06/2026  

---

## Mục lục

1. [Buổi này dạy gì và tại sao quan trọng](#1-buổi-này-dạy-gì-và-tại-sao-quan-trọng)
2. [Câu chuyện mở đầu — Khi "chạy được" chưa phải "dùng được"](#2-câu-chuyện-mở-đầu--khi-chạy-được-chưa-phải-dùng-được)
3. [Kiến trúc 5 lớp của một AI feature](#3-kiến-trúc-5-lớp-của-một-ai-feature)
4. [Trust Layer — Lớp bảo vệ bao quanh AI](#4-trust-layer--lớp-bảo-vệ-bao-quanh-ai)
5. [Vibe Coding có kỷ luật — Dùng Claude Code đúng cách](#5-vibe-coding-có-kỷ-luật--dùng-claude-code-đúng-cách)
6. [Hai ranh giới cứng — Secret & PII](#6-hai-ranh-giới-cứng--secret--pii)
7. [Fallback — Thiết kế cho lúc AI thất bại](#7-fallback--thiết-kế-cho-lúc-ai-thất-bại)
8. [Thực hành có hướng dẫn — Build feature phân loại email khiếu nại](#8-thực-hành-có-hướng-dẫn--build-feature-phân-loại-email-khiếu-nại)
9. [Lab Build — Tự xây feature của bạn (90 phút)](#9-lab-build--tự-xây-feature-của-bạn-90-phút)
10. [Code Review cặp đôi — Đọc code người khác như thế nào](#10-code-review-cặp-đôi--đọc-code-người-khác-như-thế-nào)
11. [Tóm tắt & bước tiếp theo](#11-tóm-tắt--bước-tiếp-theo)
12. [Phụ lục: Dữ liệu mẫu để thực hành](#12-phụ-lục-dữ-liệu-mẫu-để-thực-hành)

---

## 1. Buổi này dạy gì và tại sao quan trọng

### Bạn đang ở đâu trong lộ trình?

Ở buổi I4.1, bạn đã hoàn thành **canvas Product Thinking** — tức là bạn đã phân tích được một vấn đề thực tế ở Yody và xác định được một feature AI có thể giải quyết nó.

Buổi I4.2 này là bước tiếp theo: **bạn sẽ thực sự build feature đó cho chạy được**. Không phải chỉ vẽ trên giấy — mà là viết code thật, chạy được thật.

Nhưng đây là điều quan trọng nhất bạn cần nhớ từ buổi hôm nay:

> **"Chạy được" là điều kiện cần nhưng chưa đủ. Điều tạo ra giá trị thật của một builder là làm cho feature "dùng được" — tức là an toàn, tin cậy, và không vỡ khi AI trả lời sai.**

### Sau buổi này bạn sẽ làm được gì?

- Build được một AI feature nhỏ chạy từ đầu đến cuối (end-to-end)
- Hiểu và áp dụng được kiến trúc 5 lớp của một AI feature
- Thiết kế **trust layer** — lớp bảo vệ bao quanh AI — bao gồm ẩn thông tin nhạy cảm, validate input/output, và xử lý fallback
- Dùng Claude Code đúng cách — sinh code nhanh nhưng có kỷ luật, không merge mù
- Đọc và review code của người khác để phát hiện rủi ro tích hợp AI

### Tại sao kỹ năng này quan trọng với Yody?

Yody đang xây dựng nhiều feature AI trong vận hành — từ tự động phân loại khiếu nại khách hàng, gợi ý size sản phẩm, đến phân tích xu hướng tồn kho. Mỗi feature này đều xử lý dữ liệu thực của khách hàng.

Nếu một feature AI không có trust layer:
- **Thông tin khách hàng có thể bị lộ** vào trong model hoặc log hệ thống (vi phạm Luật Bảo vệ dữ liệu cá nhân 91/2025/QH15)
- **Kết quả sai từ AI** có thể được hiển thị cho người dùng hoặc gây crash hệ thống
- **API key bị lộ** có thể bị lạm dụng và gây chi phí khổng lồ

Một builder level 4 không chỉ biết gọi AI — họ biết bảo vệ hệ thống khỏi chính AI mà họ đang dùng.

---

## 2. Câu chuyện mở đầu — Khi "chạy được" chưa phải "dùng được"

> 🖼️ **[Hình minh họa cần có — `img-no-trust`]**: Một màn hình code đơn giản: một hộp input nhận nội dung khiếu nại khách hàng có tên và số điện thoại, một mũi tên thẳng vào model AI, một kết quả trả về sai định dạng, và cuối cùng là màn hình đỏ "App Crashed". Khoanh tròn đỏ tại ba điểm: (1) chỗ PII đi vào model, (2) output sai định dạng, (3) không có fallback.  
> **Prompt tạo hình:** *"A fragile AI feature: raw customer text with a phone number sent straight into a model, a malformed label output, and an app crash screen; three red circles marking the three failure points — modern flat editorial illustration meets clean sketch-note style, confident hand-drawn linework with subtle marker shading, tech/office aesthetic, brand palette deep indigo #2a2b86 and warm amber #fcaf16 on off-white background, Montserrat-like geometric sans for labels, generous whitespace, minimal, professional, 16:9."*

### Hãy nhìn vào đoạn code sau

Giả sử một bạn vừa build xong feature tự động phân loại khiếu nại khách hàng. Code trông như thế này:

```python
# ⚠️ ĐÂY LÀ CODE CÓ VẤN ĐỀ — đừng làm theo
def xu_ly_khieu_nai(noi_dung_email):
    # Gửi thẳng nội dung email vào model
    ket_qua = goi_model(noi_dung_email)
    
    # Dùng kết quả trả về trực tiếp
    return ket_qua["label"]
```

Bạn chạy thử — nó hoạt động! Nhập email khiếu nại vào, model trả về nhãn phân loại, mọi thứ trông ổn.

**Nhưng hãy dừng lại và hỏi: điều gì có thể sai?**

**Vấn đề 1: PII (thông tin định danh cá nhân) đi thẳng vào model**

Email khiếu nại của khách thường có tên, số điện thoại, địa chỉ:
> *"Tôi tên Nguyễn Văn A, SĐT 0901234567, địa chỉ 123 Trần Hưng Đạo, TP.HCM. Tôi đặt áo khoác nhưng giao sai size..."*

Toàn bộ nội dung này — bao gồm tên thật, số điện thoại thật, địa chỉ thật — đang đi thẳng vào model AI. Điều này có nghĩa là dữ liệu cá nhân của khách hàng đang nằm ngoài tầm kiểm soát của bạn. Theo Luật 91/2025/QH15 về bảo vệ dữ liệu cá nhân, đây là rủi ro pháp lý nghiêm trọng.

**Vấn đề 2: Output của model không được kiểm tra**

Model AI đôi khi trả về output không như mong đợi. Thay vì trả về `"doi_tra"`, model có thể trả về:
- `"Đổi/trả hàng"` (đúng nghĩa nhưng sai định dạng)
- `"UNKNOWN"` (không nhận ra loại khiếu nại)
- `None` (lỗi hệ thống)
- Một đoạn text dài thay vì nhãn ngắn

Khi code của bạn làm `ket_qua["label"]` mà `ket_qua` là một string thay vì dict, hệ thống sẽ crash ngay lập tức.

**Vấn đề 3: Không có đường lui (fallback)**

Nếu model timeout (hết thời gian chờ), nếu API trả lỗi, nếu model trả kết quả không tin cậy — code trên không xử lý được bất kỳ tình huống nào trong số này. Kết quả: người dùng thấy màn hình trắng hoặc lỗi hệ thống.

### Ba lỗi = Ba lý do để học trust layer

| Lỗi | Hệ quả | Thuộc về |
|-----|--------|----------|
| PII đi thẳng vào model | Rủi ro pháp lý, lộ dữ liệu khách | Trust layer — Redaction |
| Output không được kiểm tra | App crash, kết quả sai hiển thị | Trust layer — Validate output |
| Không có fallback | Hệ thống vỡ khi AI thất bại | Trust layer — Fallback |

**Đây chính xác là những gì bạn sẽ học và build trong buổi hôm nay.**

---

## 3. Kiến trúc 5 lớp của một AI feature

> 🖼️ **[Hình minh họa cần có — `img-5-layers`]**: Năm hộp xếp dọc theo chiều từ trên xuống, mỗi hộp là một lớp với tên và mô tả ngắn. Lớp 3 (Gọi model) được vẽ mỏng hơn các lớp khác để nhấn mạnh nó chỉ là một phần nhỏ. Các lớp 2, 4, 5 được tô đậm hơn với nhãn "Trust Layer".  
> **Prompt tạo hình:** *"Five stacked layers of an AI feature labeled: 1-Input, 2-Preprocess+PII-Redaction, 3-Model Call (thin), 4-Postprocess+Validate, 5-Output+Fallback; layers 2/4/5 are highlighted as 'Trust Layer'; the model call layer is visually thinner than the rest to show it's just one small part — modern flat editorial illustration meets clean sketch-note style, confident hand-drawn linework with subtle marker shading, tech/office aesthetic, brand palette deep indigo #2a2b86 and warm amber #fcaf16 on off-white background, Montserrat-like geometric sans for labels, generous whitespace, minimal, professional, 16:9."*

### Người mới nghĩ AI feature là "gọi model" — thực ra nó có 5 lớp

Khi mới tiếp cận AI, nhiều người nghĩ một AI feature chỉ là:

```
Input của người dùng → [gọi model AI] → Output
```

Nhưng thực tế, một AI feature đáng tin cậy có kiến trúc phức tạp hơn nhiều, gồm **5 lớp riêng biệt**:

---

### Lớp 1 — Input (Nhận đầu vào)

**Đây là gì?** Điểm đầu tiên nhận dữ liệu từ người dùng hoặc hệ thống — có thể là một form điền, một file upload, một chuỗi text, hoặc dữ liệu từ database.

**Ví dụ trong bối cảnh Yody:**
- Nội dung email khiếu nại của khách hàng gửi vào
- Mô tả sản phẩm mà nhân viên nhập vào hệ thống
- Câu hỏi của khách hàng trên chatbot

**Lưu ý quan trọng:** Tại lớp này, bạn chưa làm gì với dữ liệu — bạn chỉ nhận nó. Nhưng đây là nơi bạn cần biết dữ liệu có những gì để chuẩn bị xử lý ở lớp tiếp theo.

---

### Lớp 2 — Tiền xử lý + Redaction PII (Xử lý trước khi gửi model)

**Đây là gì?** Lớp này thực hiện hai việc: (1) kiểm tra và làm sạch dữ liệu đầu vào, và (2) **ẩn tất cả thông tin định danh cá nhân** trước khi dữ liệu chạm vào model AI.

**Tại sao quan trọng?** Đây là lớp bảo vệ đầu tiên. Một khi dữ liệu đã vào model, bạn không kiểm soát được nó nữa. Vì vậy, bạn phải lọc sạch PII *trước khi* gửi đi.

**Những gì cần làm ở lớp này:**

1. **Validate input** — Kiểm tra dữ liệu có đúng định dạng không? Có quá ngắn/quá dài không? Có chứa ký tự bất hợp lệ không?
   
2. **Redact PII (ẩn thông tin định danh)** — Thay thế các thông tin nhạy cảm bằng placeholder:
   - Số điện thoại → `[SĐT]`
   - Địa chỉ email → `[EMAIL]`
   - Số CCCD/CMND → `[CCCD]`
   - Họ tên (ở bản nâng cấp) → `[TÊN]`

**Ví dụ:** Câu gốc:
> *"Tôi là Trần Văn Demo, SĐT 0901234567, email demo@example.com. Áo của tôi bị lỗi."*

Sau khi qua lớp 2:
> *"Tôi là [TÊN], SĐT [SĐT], email [EMAIL]. Áo của tôi bị lỗi."*

Dữ liệu sau redaction giữ nguyên nghĩa để model hiểu được, nhưng không còn thông tin định danh thật của khách.

**Nếu bỏ lớp này:** Dữ liệu cá nhân của khách hàng sẽ đi thẳng vào model AI — rủi ro pháp lý và đạo đức.

---

### Lớp 3 — Gọi model (Model Call)

**Đây là gì?** Đây là lớp thực sự "nói chuyện" với AI. Bạn gửi dữ liệu đã được xử lý ở lớp 2 lên model, và nhận kết quả về.

**Điều quan trọng cần hiểu:** Đây là lớp **ngắn nhất và đơn giản nhất** trong toàn bộ kiến trúc. Chỉ vài dòng code. Phần phức tạp và có giá trị nằm ở các lớp khác.

**Best practice ở lớp này:**

- **Dùng structured output** — Yêu cầu model trả về định dạng cố định (JSON) thay vì text tự do. Điều này giúp lớp 4 validate dễ hơn nhiều.
  
  Ví dụ, thay vì để model trả về *"Đây là khiếu nại về đổi trả hàng"*, bạn yêu cầu model trả về:
  ```json
  {"nhan": "doi_tra", "do_tin_cay": 0.92}
  ```

- **Đặt system prompt rõ ràng** — Cho model biết chính xác nó cần làm gì, tập nhãn được phép là gì, và định dạng trả về như thế nào.

- **Không hardcode API key** — Key cần được đọc từ biến môi trường (xem Phần 6).

**Nếu bỏ structured output:** Lớp 4 sẽ rất khó validate vì không biết trước kết quả trông như thế nào.

---

### Lớp 4 — Hậu xử lý + Validate Output (Kiểm tra kết quả)

**Đây là gì?** Lớp này nhận output từ model và kiểm tra xem nó có đúng như mong đợi không trước khi cho qua.

**Tại sao quan trọng?** Model AI không phải lúc nào cũng trả về đúng những gì bạn muốn. Ngay cả khi bạn đã yêu cầu rõ ràng, model vẫn có thể:
- Trả về một nhãn không có trong tập cho phép
- Trả về độ tin cậy rất thấp (tức là model đang đoán mò)
- Trả về JSON không hợp lệ
- Trả về rỗng

**Những gì cần kiểm tra:**

1. **Định dạng có đúng không?** — Output có phải JSON không? Có đủ các trường cần thiết không?
2. **Giá trị có hợp lệ không?** — Nhãn có nằm trong tập cho phép không? Độ tin cậy có trên ngưỡng tối thiểu không?
3. **Nội dung có an toàn không?** — Output có chứa PII hay nội dung không phù hợp không?

**Ví dụ kiểm tra:**

```python
TAP_NHAN_HOP_LE = {"doi_tra", "giao_hang", "chat_luong", "khac"}
NGUONG_TIN_CAY_TOI_THIEU = 0.6  # Model phải ít nhất 60% chắc chắn

def ket_qua_co_hop_le(r):
    # Kiểm tra định dạng
    if not isinstance(r, dict):
        return False
    # Kiểm tra nhãn có trong tập cho phép
    if r.get("nhan") not in TAP_NHAN_HOP_LE:
        return False
    # Kiểm tra độ tin cậy
    if r.get("do_tin_cay", 0) < NGUONG_TIN_CAY_TOI_THIEU:
        return False
    return True
```

**Nếu bỏ lớp này:** Output sai từ model sẽ đi thẳng ra người dùng hoặc vào database — có thể gây ra quyết định sai, crash hệ thống, hoặc dữ liệu bẩn.

---

### Lớp 5 — Output + Fallback (Trả kết quả và xử lý khi thất bại)

**Đây là gì?** Lớp cuối cùng — trả kết quả cho người dùng nếu mọi thứ ổn, và xử lý các tình huống thất bại một cách duyên dáng nếu có vấn đề.

**Tại sao quan trọng?** Đây là lớp định nghĩa *trải nghiệm người dùng khi AI sai*. Có hai cách xử lý:
- **Tệ:** Để app crash, hiển thị lỗi kỹ thuật khó hiểu, hoặc im lặng không làm gì
- **Tốt:** Thông báo rõ ràng, tử tế, và đưa ra giải pháp thay thế

**Fallback tốt cho Yody:** Với feature phân loại khiếu nại, fallback đẹp là:
> "Yêu cầu này cần người xử lý" → Chuyển ticket cho nhân viên CSKH

Thay vì: gán bừa một nhãn nào đó, để app crash, hoặc không làm gì cả.

---

### Tóm tắt kiến trúc 5 lớp

| Lớp | Tên | Làm gì | Bỏ lớp này → rủi ro gì |
|-----|-----|---------|------------------------|
| 1 | **Input** | Nhận dữ liệu đầu vào | — (không có rủi ro riêng) |
| 2 | **Tiền xử lý + Redaction** | Validate input, ẩn PII trước khi gửi model | Lộ PII vào model và log |
| 3 | **Gọi model** | Gửi dữ liệu đã xử lý lên AI, nhận kết quả | — (phần đơn giản nhất) |
| 4 | **Hậu xử lý + Validate** | Kiểm tra output đúng định dạng và hợp lệ | Output sai lọt ra người dùng, app crash |
| 5 | **Output + Fallback** | Trả kết quả; xử lý khi AI thất bại | Trải nghiệm người dùng tệ khi AI sai |

> **Điểm cốt lõi:** Trong 5 lớp, **4 lớp thuộc về trust layer** (lớp 2, 4, 5 và một phần lớp 3). Chỉ một lớp là thực sự "gọi AI". Đây là tỷ lệ phản ánh đúng giá trị thật của công việc một builder.

---

## 4. Trust Layer — Lớp bảo vệ bao quanh AI

> 🖼️ **[Hình minh họa cần có — `img-trust-layer`]**: Một hình tròn ở giữa với chữ "Model AI". Bao quanh là một vòng tròn lớn hơn được chia thành 4 phần, mỗi phần là một thành phần của trust layer: "Validate Input", "Giới hạn phạm vi", "Kiểm Output", "Fallback". Màu sắc: deep indigo cho model, amber cho trust layer ring.  
> **Prompt tạo hình:** *"A model in the center wrapped by a trust-layer ring divided into four labeled segments: Validate Input, Scope-Limit, Check Output, Fallback; the center model is smaller, the trust layer ring is the dominant visual element — modern flat editorial illustration meets clean sketch-note style, confident hand-drawn linework with subtle marker shading, tech/office aesthetic, brand palette deep indigo #2a2b86 and warm amber #fcaf16 on off-white background, Montserrat-like geometric sans for labels, generous whitespace, minimal, professional, 16:9."*

### Trust layer là gì?

**Trust layer không phải là bản thân model AI.** Đó là toàn bộ code bao quanh model — code giúp bạn kiểm soát những gì đi vào model và những gì đi ra từ model.

Hãy tưởng tượng model AI như một nhân viên mới rất thông minh nhưng đôi khi không lường trước được mọi tình huống:
- Đôi khi họ hiểu sai yêu cầu
- Đôi khi họ trả lời không đúng định dạng
- Đôi khi họ cần nghỉ (timeout)
- Và bạn không muốn họ thấy thông tin mật không cần thiết

**Trust layer là tập hợp các quy trình và kiểm tra** giúp bạn làm việc an toàn và hiệu quả với "nhân viên" này.

### Bốn thành phần của trust layer

**1. Validate Input — Kiểm tra đầu vào**

Trước khi làm bất cứ điều gì, hãy kiểm tra dữ liệu nhận được có hợp lệ không.

```python
def validate_input(noi_dung: str) -> bool:
    # Không được rỗng
    if not noi_dung or noi_dung.strip() == "":
        return False
    # Không quá ngắn (ít nhất 10 ký tự)
    if len(noi_dung) < 10:
        return False
    # Không quá dài (tối đa 2000 ký tự để tránh spam/injection)
    if len(noi_dung) > 2000:
        return False
    return True
```

**Câu hỏi cần tự hỏi:** Input có thể là gì? Cái gì sẽ làm model xử lý sai? Làm thế nào để từ chối đầu vào không hợp lệ trước khi tốn API call?

---

**2. Giới hạn phạm vi — Scope Limiting**

Model AI có thể làm nhiều thứ, nhưng trong feature của bạn, nó chỉ nên làm một việc cụ thể. Việc giới hạn phạm vi giúp kết quả nhất quán và an toàn hơn.

Thực hiện bằng cách viết system prompt rõ ràng, hạn chế:

```python
SYSTEM_PROMPT = """
Bạn là hệ thống phân loại khiếu nại khách hàng của Yody.

NHIỆM VỤ DUY NHẤT: Phân loại nội dung khách hàng gửi vào đúng một trong các nhóm sau:
- doi_tra: Yêu cầu đổi hoặc trả hàng
- giao_hang: Vấn đề liên quan đến giao hàng, vận chuyển
- chat_luong: Phản hồi về chất lượng sản phẩm
- khac: Các nội dung khác

KHÔNG được:
- Trả lời câu hỏi nào khác ngoài phân loại
- Đề xuất giải pháp hoặc trả lời trực tiếp khách hàng
- Bình luận về thông tin cá nhân trong nội dung

CHỈ trả về JSON theo đúng định dạng:
{"nhan": "<một trong bốn nhãn trên>", "do_tin_cay": <0.0 đến 1.0>}
"""
```

---

**3. Kiểm tra output — Output Validation**

Sau khi nhận kết quả từ model, kiểm tra trước khi dùng. Không bao giờ tin tưởng mù quáng vào output của AI.

```python
TAP_NHAN_HOP_LE = {"doi_tra", "giao_hang", "chat_luong", "khac"}

def ket_qua_hop_le(r) -> bool:
    """Kiểm tra output từ model có đúng định dạng và hợp lệ không."""
    # Phải là dict
    if not isinstance(r, dict):
        return False
    # Phải có trường 'nhan'
    if "nhan" not in r:
        return False
    # Nhãn phải nằm trong tập cho phép
    if r["nhan"] not in TAP_NHAN_HOP_LE:
        return False
    # Độ tin cậy phải trên ngưỡng tối thiểu
    if r.get("do_tin_cay", 0) < 0.6:
        return False
    return True
```

---

**4. Fallback — Đường lui an toàn**

Khi mọi thứ không như ý (model sai, model trả rỗng, service timeout), phải có một đường lui an toàn thay vì để hệ thống vỡ.

```python
def xu_ly_voi_fallback(noi_dung: str) -> str:
    """Trả về nhãn phân loại, hoặc fallback nếu không chắc chắn."""
    try:
        ket_qua = goi_model_va_parse(noi_dung)
        if ket_qua_hop_le(ket_qua):
            return ket_qua["nhan"]       # Trả kết quả tốt
        else:
            return "can_nguoi_xu_ly"     # Fallback: model không chắc → người xử lý
    except TimeoutError:
        return "can_nguoi_xu_ly"         # Fallback: service chậm/timeout
    except Exception:
        return "can_nguoi_xu_ly"         # Fallback: lỗi không lường trước
```

### Câu hỏi phân biệt builder có kỷ luật

| Builder mới hỏi | Builder có kỷ luật hỏi |
|-----------------|------------------------|
| "Feature chạy được chưa?" | "Khi AI trả về sai định dạng thì sao?" |
| "Code có chạy không?" | "PII đã được ẩn trước khi vào model chưa?" |
| "Test case basic pass chưa?" | "Khi service timeout, người dùng thấy gì?" |
| "Output đúng chưa?" | "API key có đang bị lộ trong code không?" |

---

## 5. Vibe Coding có kỷ luật — Dùng Claude Code đúng cách

> 🖼️ **[Hình minh họa cần có — `img-vibe-coding`]**: Hai cảnh song song. Bên trái (SAI): người dùng nhìn Claude Code generate code và bấm "Merge" ngay không đọc, với dấu X màu đỏ. Bên phải (ĐÚNG): người dùng đọc từng đoạn code, có ghi chú "Đọc & hiểu → rồi mới giữ", với dấu tick màu xanh. Phía dưới có chú thích: "Tốc độ ≠ thay thế trách nhiệm".  
> **Prompt tạo hình:** *"Split scene: left side shows a developer blindly merging AI-generated code without reading (red X); right side shows a developer carefully reading each changed line before accepting (green checkmark); caption reads 'Speed does not replace responsibility, no blind merge' — modern flat editorial illustration meets clean sketch-note style, confident hand-drawn linework with subtle marker shading, tech/office aesthetic, brand palette deep indigo #2a2b86 and warm amber #fcaf16 on off-white background, Montserrat-like geometric sans for labels, generous whitespace, minimal, professional, 16:9."*

### Claude Code là gì?

**Claude Code** là công cụ dòng lệnh (CLI) của Anthropic cho phép bạn dùng AI trực tiếp để viết code, chỉnh sửa file, chạy lệnh, và build feature — ngay trong terminal của bạn.

Thay vì tự viết từng dòng code, bạn mô tả yêu cầu bằng ngôn ngữ tự nhiên, và Claude Code sinh code ra cho bạn. Ví dụ:

> *"Viết hàm Python nhận một string text, dùng regex để ẩn số điện thoại Việt Nam (10 số bắt đầu bằng 0) và thay bằng [SĐT]. Trả về string đã xử lý."*

Claude Code sẽ viết hàm đó cho bạn trong vài giây.

### Sức mạnh và cái bẫy của Claude Code

**Sức mạnh:** Claude Code sinh code rất nhanh. Bạn có thể build feature trong vài giờ thay vì vài ngày.

**Cái bẫy:** Chính vì code được sinh ra quá nhanh, có xu hướng muốn chấp nhận hết và "merge" (đưa vào project) ngay mà không đọc kỹ. Đây là **"merge mù"** — và đây là thói quen nguy hiểm.

### Nguyên tắc vàng: Đọc-Hiểu trước khi Giữ

**Quy tắc cơ bản:** Bạn phải đọc-hiểu từng đoạn code Claude Code sinh ra trước khi giữ lại. Nếu bạn không giải thích được một đoạn code làm gì, bạn chưa sẵn sàng dùng nó.

**Lý do không chỉ là "best practice":**
- Claude Code không hiểu bối cảnh business của bạn — nó có thể sinh code hoạt động về mặt kỹ thuật nhưng sai về nghiệp vụ
- Claude Code có thể bỏ sót edge case quan trọng với data của Yody
- Claude Code không biết trust layer của bạn cần làm gì — nếu bạn không yêu cầu cụ thể, nó sẽ bỏ qua
- Bạn là người chịu trách nhiệm cuối cùng, không phải AI

### Workflow đúng khi dùng Claude Code

**Bước 1 — Yêu cầu rõ ràng và có phạm vi**

Thay vì: *"Viết code phân loại email khiếu nại cho tôi"*

Hãy dùng: *"Viết hàm Python `redact(text: str) -> str` nhận một string, dùng regex để ẩn: (1) số điện thoại VN 10 số bắt đầu bằng 0 → `[SĐT]`, (2) địa chỉ email → `[EMAIL]`, (3) số CCCD 12 chữ số → `[CCCD]`. Trả về string đã ẩn. Thêm docstring giải thích."*

Yêu cầu càng cụ thể, kết quả càng sát với mong đợi và dễ review hơn.

**Bước 2 — Đọc code được sinh ra**

Khi Claude Code sinh ra một đoạn code, hãy đọc từng dòng và tự hỏi:
- Dòng này làm gì?
- Tại sao cần dòng này?
- Có edge case nào chưa được xử lý không?
- Có import/thư viện nào lạ không?

**Bước 3 — Test với dữ liệu thật**

Chạy code với các ca cụ thể để kiểm tra:
- Ca bình thường hoạt động không?
- Ca có PII: PII có bị ẩn đúng không?
- Ca mơ hồ: fallback có kích hoạt không?
- Ca lỗi: exception có được bắt không?

**Bước 4 — Chỉ giữ lại sau khi hiểu và test**

Sau khi đã đọc, hiểu, và test — bạn mới accept/merge đoạn code đó.

### Cách yêu cầu Claude Code hiệu quả

**Chia nhỏ yêu cầu:** Thay vì yêu cầu build toàn bộ feature một lần, hãy build từng lớp:
1. Trước: *"Build hàm redact PII"*
2. Sau: *"Build hàm gọi model với structured output"*
3. Sau nữa: *"Build hàm validate output"*
4. Cuối: *"Ghép ba hàm trên vào hàm chính với fallback"*

Cách này giúp bạn kiểm soát từng bước và dễ debug hơn.

**Yêu cầu giải thích:** Khi Claude Code sinh code phức tạp, hãy hỏi thêm: *"Giải thích regex này làm gì?"* hoặc *"Tại sao dùng try/except ở đây?"*

**Yêu cầu test case:** *"Viết 3 test case cho hàm vừa viết, bao gồm ca có SĐT, ca không có PII, và ca rỗng."*

---

## 6. Hai ranh giới cứng — Secret & PII

> 🖼️ **[Hình minh họa cần có — `img-secret-pii`]**: Ba ô vuông, mỗi ô là một quy tắc với biểu tượng rõ ràng. Ô 1: hình API key với dấu cấm và hình biến môi trường (.env) với dấu tích — "Không hardcode key". Ô 2: hình data khách hàng → hộp redaction → model (mũi tên) — "Ẩn PII trước khi gửi". Ô 3: hình log file với kính lúp kiểm tra PII — "Log sạch, không PII".  
> **Prompt tạo hình:** *"Three hard rules shown as three panels: panel 1 shows crossed-out hardcoded API key vs approved environment variable; panel 2 shows customer data going through a redaction box before reaching the model; panel 3 shows a clean log file being inspected — modern flat editorial illustration meets clean sketch-note style, confident hand-drawn linework with subtle marker shading, tech/office aesthetic, brand palette deep indigo #2a2b86 and warm amber #fcaf16 on off-white background, Montserrat-like geometric sans for labels, generous whitespace, minimal, professional, 16:9."*

Đây là hai chủ đề bạn không được thương lượng hay bỏ qua trong bất kỳ buổi lab nào, bất kỳ feature nào bạn build ở Yody. Vi phạm một trong hai điều này là **lỗi chặn ĐẠT** — tức là buổi học này **không được tính là ĐẠT**, không phải chỉ trừ điểm.

### Ranh giới 1 — Secret: API Key không được hardcode

#### API Key là gì?

Khi bạn dùng Claude API (hoặc bất kỳ AI API nào), bạn cần một **API key** — một chuỗi ký tự dài được dùng để xác thực danh tính của bạn với nhà cung cấp. Mỗi lần gọi API, key này được gửi đi để chứng minh bạn có quyền dùng service.

API key giống như mật khẩu tài khoản ngân hàng — ai có được key của bạn có thể dùng API với danh nghĩa của bạn, và chi phí sẽ tính vào tài khoản của bạn.

#### Vấn đề khi hardcode API key

**Hardcode** có nghĩa là viết key trực tiếp vào code:

```python
# ⚠️ TUYỆT ĐỐI KHÔNG LÀM NÀY
api_key = "sk-ant-api03-abc123def456..."  # hardcoded key

response = anthropic.messages.create(
    model="claude-opus-4-5",
    api_key=api_key,  # Lộ key trong code
    ...
)
```

**Tại sao nguy hiểm?**
- Nếu bạn commit code này lên GitHub (dù là repo private), key có thể bị lộ
- Nếu bạn chia sẻ file code, key bị lộ
- GitHub và các nền tảng khác có bot tự động quét key — key của bạn có thể bị tìm và lạm dụng trong vài phút sau khi commit

#### Cách đúng: Biến môi trường (Environment Variable)

**Biến môi trường** là các giá trị được lưu trên máy tính hoặc server của bạn, bên ngoài code. Code của bạn đọc giá trị từ biến môi trường thay vì có key trực tiếp trong code.

**Bước 1:** Tạo file `.env` trong thư mục project (file này KHÔNG commit lên git):

```
# File: .env (KHÔNG commit file này)
CLAUDE_API_KEY=sk-ant-api03-abc123def456...
MODEL_NAME=claude-opus-4-5
```

**Bước 2:** Thêm `.env` vào `.gitignore`:

```
# File: .gitignore
.env
.env.local
.env.production
```

**Bước 3:** Đọc key trong code bằng `os.environ`:

```python
import os
from dotenv import load_dotenv  # thư viện python-dotenv

# Đọc file .env
load_dotenv()

# Đọc key từ biến môi trường — KHÔNG hardcode
api_key = os.environ.get("CLAUDE_API_KEY")
model_name = os.environ.get("MODEL_NAME", "claude-opus-4-5")  # có default value

if not api_key:
    raise ValueError("CLAUDE_API_KEY không được cấu hình")
```

**Cách kiểm tra bạn đã làm đúng chưa:**
- Tìm kiếm trong code bằng: `grep -r "sk-ant" .` — nếu tìm thấy key, bạn đang hardcode
- Kiểm tra `.gitignore` có chứa `.env` không
- Kiểm tra bạn có đang commit file `.env` không: `git status`

---

### Ranh giới 2 — PII: Ẩn thông tin định danh cá nhân

#### PII là gì?

**PII (Personally Identifiable Information)** — thông tin định danh cá nhân — là bất kỳ thông tin nào có thể được dùng để nhận dạng một người cụ thể, trực tiếp hoặc kết hợp.

**Các loại PII cần đặc biệt chú ý trong bối cảnh Yody:**

| Loại PII | Ví dụ | Placeholder khi redact |
|----------|-------|------------------------|
| Số điện thoại | 0901234567 | `[SĐT]` |
| Địa chỉ email | khachhang@gmail.com | `[EMAIL]` |
| Số CCCD/CMND | 012345678901 | `[CCCD]` |
| Họ và tên đầy đủ | Nguyễn Văn A | `[TÊN]` |
| Địa chỉ nhà | 123 Trần Hưng Đạo, Q1, TP.HCM | `[ĐỊA CHỈ]` |
| Mã đơn hàng kết hợp với thông tin trên | #YD20260625-001 | cần đánh giá ngữ cảnh |

**Lưu ý quan trọng về "tổ hợp định danh":** Đôi khi một thông tin riêng lẻ không đủ để nhận dạng ai, nhưng kết hợp lại thì có thể. Ví dụ: tên "Nguyễn Văn A" + "cư dân quận 1" + "mua váy size M" — kết hợp ba thông tin này có thể đủ để định danh. Vì vậy, cách an toàn nhất là **ẩn tất cả PII**, không chỉ ẩn từng loại riêng lẻ.

#### Tại sao phải ẩn PII trước khi gửi vào model?

Có hai lý do chính:

**Lý do pháp lý:** Theo Luật Bảo vệ dữ liệu cá nhân 91/2025/QH15 (có hiệu lực từ 01/01/2026) và Nghị định 356/2025/NĐ-CP, dữ liệu cá nhân chỉ được xử lý với sự đồng ý của chủ thể dữ liệu và đúng với mục đích đã đăng ký. Việc đưa PII của khách hàng vào một model AI bên thứ ba mà không có đồng ý rõ ràng là vi phạm pháp luật.

**Lý do kỹ thuật:** Dữ liệu gửi vào model AI có thể được lưu trong log của nhà cung cấp, được dùng để huấn luyện model (tùy điều khoản dịch vụ), hoặc xuất hiện trong các cuộc hội thoại khác. Một khi dữ liệu ra khỏi hệ thống của bạn, bạn không kiểm soát được nó nữa.

#### Cách implement redaction

Dưới đây là hàm redact cơ bản dùng regex. **Lưu ý:** Đây là bản đơn giản cho lab — bản production cần thêm NER (Named Entity Recognition) để nhận dạng tên người và địa chỉ.

```python
import re

def redact_pii(text: str) -> str:
    """
    Ẩn thông tin định danh cá nhân (PII) trong text.
    
    Thay thế:
    - Số điện thoại VN (10 số, bắt đầu bằng 0) → [SĐT]
    - Địa chỉ email → [EMAIL]  
    - Số CCCD/CMND (12 chữ số) → [CCCD]
    
    Lưu ý: bản này dùng regex cơ bản. Để ẩn tên người và địa chỉ,
    cần dùng NER (Named Entity Recognition) ở bản production.
    
    Returns:
        str: Text đã được ẩn PII
    """
    # Ẩn số điện thoại VN: 10 số bắt đầu bằng 0
    # Ví dụ: 0901234567, 0312345678
    text = re.sub(r"\b0\d{9}\b", "[SĐT]", text)
    
    # Ẩn địa chỉ email
    # Ví dụ: khach@gmail.com, user.name+tag@company.co.uk
    text = re.sub(r"[\w.+\-]+@[\w\-]+\.[\w.\-]+", "[EMAIL]", text)
    
    # Ẩn số CCCD/CMND (12 chữ số liên tiếp)
    text = re.sub(r"\b\d{12}\b", "[CCCD]", text)
    
    return text
```

**Kiểm tra hàm redact hoạt động đúng:**

```python
# Test cases
test_cases = [
    # Ca 1: Có SĐT
    ("SĐT của tôi là 0901234567, xin liên hệ", 
     "SĐT của tôi là [SĐT], xin liên hệ"),
    
    # Ca 2: Có email
    ("Email của tôi: khachhang@gmail.com để xác nhận",
     "Email của tôi: [EMAIL] để xác nhận"),
    
    # Ca 3: Có CCCD
    ("CCCD của tôi 012345678901, xác minh giúp",
     "CCCD của tôi [CCCD], xác minh giúp"),
    
    # Ca 4: Kết hợp nhiều PII
    ("Tôi tên A, SĐT 0912345678, email a@b.com",
     "Tôi tên A, SĐT [SĐT], email [EMAIL]"),
     # Lưu ý: tên "A" chưa bị ẩn vì regex không xử lý tên người
    
    # Ca 5: Không có PII
    ("Áo bị lỗi chỉ sau một lần giặt, chất lượng kém",
     "Áo bị lỗi chỉ sau một lần giặt, chất lượng kém"),
]

for input_text, expected in test_cases:
    result = redact_pii(input_text)
    status = "✓ PASS" if result == expected else "✗ FAIL"
    print(f"{status}: '{input_text[:40]}...' → '{result[:40]}...'")
```

#### Ba quy tắc bắt buộc về PII và Secret

> **Quy tắc 1:** Không bao giờ hardcode API key vào code. Luôn dùng biến môi trường.
> 
> **Quy tắc 2:** Luôn chạy `redact_pii()` với input trước khi gọi model. Không có ngoại lệ.
> 
> **Quy tắc 3:** Log và output không được chứa PII. Kiểm tra lại sau khi thêm log statement.

---

## 7. Fallback — Thiết kế cho lúc AI thất bại

> 🖼️ **[Hình minh họa cần có — `img-fallback`]**: Ba làn đường (lanes) song song, mỗi làn là một loại thất bại. Làn 1: "Output sai định dạng" → mũi tên → "Chuyển người xử lý". Làn 2: "Model trả rỗng" → mũi tên → "Thông báo lỗi thân thiện". Làn 3: "Service timeout" → mũi tên → "Thử lại / Hàng đợi". Cuối mỗi làn là gương mặt người dùng hài lòng, không phải màn hình lỗi.  
> **Prompt tạo hình:** *"Three parallel failure lanes: lane 1 shows wrong format output routing to a human agent; lane 2 shows empty model response routing to a friendly error message; lane 3 shows service timeout routing to retry/queue; each lane ends with a satisfied user face instead of an error screen — modern flat editorial illustration meets clean sketch-note style, confident hand-drawn linework with subtle marker shading, tech/office aesthetic, brand palette deep indigo #2a2b86 and warm amber #fcaf16 on off-white background, Montserrat-like geometric sans for labels, generous whitespace, minimal, professional, 16:9."*

### Tại sao cần thiết kế fallback?

Một quan niệm sai phổ biến: *"Feature AI tốt là feature AI luôn đúng."*

Sự thật: **AI sẽ có lúc sai, lúc không chắc, lúc không phản hồi.** Đây không phải bug — đây là đặc tính cơ bản của AI. Model xác suất không thể đảm bảo 100% chính xác trong mọi tình huống.

**Feature AI tốt là feature vẫn dùng được hoặc báo lỗi tử tế khi AI thất bại.** Đây là điều phân biệt một feature production-ready với một demo.

### Ba loại thất bại cần xử lý

#### Loại 1: Output sai định dạng

**Khi nào xảy ra?** Model trả về text tự do thay vì JSON, hoặc JSON thiếu trường, hoặc JSON không parse được.

**Ví dụ:** Bạn yêu cầu model trả `{"nhan": "doi_tra", "do_tin_cay": 0.9}` nhưng nhận được:
- `"Đây là khiếu nại về đổi trả."` (text tự do)
- `{"label": "return", "confidence": 0.9}` (tên trường sai)
- `{nhan: doi_tra}` (JSON không hợp lệ — thiếu dấu nháy)

**Cách xử lý:**
```python
import json

def parse_output_model(raw_response: str) -> dict | None:
    """
    Parse output từ model. Trả về dict nếu hợp lệ, None nếu không parse được.
    """
    try:
        # Thử parse JSON
        data = json.loads(raw_response)
        return data
    except json.JSONDecodeError:
        # Model trả về text tự do, không phải JSON
        # Log lại để debug sau
        print(f"[WARN] Model trả về không phải JSON: {raw_response[:100]}")
        return None
```

---

#### Loại 2: Output hợp lệ nhưng không tin cậy

**Khi nào xảy ra?** Model trả về JSON đúng định dạng nhưng độ tin cậy thấp — tức là model đang "đoán mò".

**Ví dụ:** `{"nhan": "doi_tra", "do_tin_cay": 0.35}` — model chỉ 35% chắc đây là yêu cầu đổi/trả.

**Tại sao cần xử lý?** Nếu bạn dùng kết quả với độ tin cậy thấp, bạn đang tự tin vào một quyết định mà chính AI cũng không chắc. Sẽ tốt hơn nhiều nếu chuyển cho người xử lý khi AI không chắc.

```python
NGUONG_TIN_CAY = 0.6  # 60% — ngưỡng tối thiểu để chấp nhận kết quả

def ket_qua_du_tin_cay(ket_qua: dict) -> bool:
    return ket_qua.get("do_tin_cay", 0) >= NGUONG_TIN_CAY
```

**Lưu ý về việc chọn ngưỡng:** Ngưỡng 0.6 là điểm xuất phát tốt cho hầu hết use case. Tuy nhiên, trong thực tế bạn cần điều chỉnh dựa trên:
- **Chi phí của sai lầm:** Nếu phân loại sai gây ra thiệt hại lớn, tăng ngưỡng lên 0.8–0.9
- **Volume fallback:** Nếu quá nhiều case bị fallback, xem xét giảm ngưỡng hoặc cải thiện prompt

---

#### Loại 3: Service timeout hoặc lỗi kết nối

**Khi nào xảy ra?** API không phản hồi trong thời gian cho phép, hoặc có lỗi mạng.

**Ví dụ thực tế:** Trong giờ cao điểm, API có thể mất 10-30 giây để phản hồi. Nếu người dùng đang chờ, đây là trải nghiệm tệ. Nếu timeout sau 30 giây, code của bạn phải xử lý được.

```python
import anthropic

def goi_model_voi_timeout(prompt: str, timeout_giac: int = 10) -> dict | None:
    """
    Gọi model với timeout. Trả về None nếu timeout hoặc lỗi.
    
    Args:
        prompt: Nội dung cần phân loại (đã redact PII)
        timeout_giac: Số giây chờ tối đa (mặc định 10 giây)
    
    Returns:
        dict nếu thành công, None nếu thất bại
    """
    try:
        client = anthropic.Anthropic(api_key=os.environ.get("CLAUDE_API_KEY"))
        
        response = client.messages.create(
            model=os.environ.get("MODEL_NAME", "claude-opus-4-5"),
            max_tokens=100,
            system=SYSTEM_PROMPT,
            messages=[{"role": "user", "content": prompt}],
            timeout=timeout_giac  # Đặt timeout
        )
        
        # Extract text từ response
        raw_text = response.content[0].text
        return json.loads(raw_text)
        
    except anthropic.APITimeoutError:
        print("[WARN] API timeout — chuyển fallback")
        return None
    except anthropic.APIError as e:
        print(f"[WARN] API error: {e} — chuyển fallback")
        return None
    except json.JSONDecodeError:
        print(f"[WARN] Response không phải JSON — chuyển fallback")
        return None
```

### Fallback là quyết định của Product, không phải của kỹ thuật

Khi thiết kế fallback, câu hỏi không chỉ là *"Làm thế nào để code không crash?"* mà còn là *"Khi AI thất bại, người dùng nên thấy gì?"*

**Các option fallback phổ biến:**

| Tình huống | Fallback tệ | Fallback tốt |
|------------|------------|--------------|
| Model không chắc | Gán bừa nhãn phổ biến nhất | "Cần xem xét thêm" → chuyển người |
| API timeout | App crash / màn hình trắng | "Đang xử lý, vui lòng thử lại sau" |
| Model trả sai định dạng | Hiện lỗi kỹ thuật | Thông báo thân thiện, thử lại |
| Rate limit | Hàng đợi vô hạn | Thông báo thời gian chờ ước tính |

**Nguyên tắc thiết kế fallback tốt:**
1. **Không bao giờ crash** — người dùng phải thấy *gì đó* có nghĩa
2. **Không gán bừa** — tốt hơn là thừa nhận không chắc còn hơn đưa thông tin sai
3. **Chuyển cho người khi cần** — với các case quan trọng, fallback tốt nhất là escalate cho người thật
4. **Log đầy đủ** — để bạn biết fallback xảy ra bao nhiêu lần và vì sao
5. **Cân nhắc chi phí vận hành** — nếu ngưỡng quá cao, quá nhiều case rơi vào fallback sẽ khiến đội ngũ CSKH/operator bị quá tải. Mục tiêu là **tỷ lệ fallback hợp lý**, không phải "càng nhiều càng tốt".

---

## 8. Thực hành có hướng dẫn — Build feature phân loại email khiếu nại

> 🖼️ **[Hình minh họa cần có — `img-demo-arch`]**: Sơ đồ kiến trúc theo chiều ngang: ô "Email Khiếu Nại" → ô "Redact PII" (lớp 2) → ô "Model Phân Loại" (lớp 3, nhỏ hơn) → ô "Validate Nhãn" (lớp 4) → hai nhánh: "Nhãn Hợp Lệ" và "Cần Người Xử Lý" (lớp 5 fallback).  
> **Prompt tạo hình:** *"Architecture diagram: complaint email box → redact PII box → model classify box (smaller, labeled 'just one layer') → validate label box → two branches: valid label output and 'needs human' fallback; arrows connect each box; highlight the trust layer boxes in amber — modern flat editorial illustration meets clean sketch-note style, confident hand-drawn linework with subtle marker shading, tech/office aesthetic, brand palette deep indigo #2a2b86 and warm amber #fcaf16 on off-white background, Montserrat-like geometric sans for labels, generous whitespace, minimal, professional, 16:9."*

### Bài toán: Feature phân loại email khiếu nại của Yody

**Vấn đề thực tế:** Team CSKH của Yody nhận hàng trăm email khiếu nại mỗi ngày. Việc đọc và phân loại từng email thủ công mất nhiều thời gian và dễ nhầm lẫn. Một AI feature có thể tự động phân loại email vào đúng nhóm để routing đến đúng bộ phận xử lý.

**Kết quả mong đợi:** Nhập nội dung email vào → nhận về một trong các nhãn: `doi_tra` (đổi/trả), `giao_hang` (giao hàng), `chat_luong` (chất lượng), `khac` (khác), hoặc `can_nguoi_xu_ly` (fallback khi không chắc).

**Kiến trúc:**
```
Email khiếu nại 
    ↓
[Lớp 2] Redact PII (ẩn SĐT, email, CCCD)
    ↓
[Lớp 3] Gọi model (structured output JSON)
    ↓
[Lớp 4] Validate nhãn (có trong tập cho phép? độ tin cậy đủ?)
    ↓
[Lớp 5] Trả nhãn / Fallback "cần người xử lý"
```

### Bước 1 — Chuẩn bị môi trường

**1.1 Cài đặt thư viện cần thiết:**

```bash
pip install anthropic python-dotenv
```

**1.2 Tạo file `.env` (trong thư mục project, KHÔNG commit):**

```
CLAUDE_API_KEY=<key được cấp trong sandbox>
MODEL_NAME=claude-opus-4-5
```

**1.3 Tạo file `.gitignore` (nếu chưa có):**

```
.env
__pycache__/
*.pyc
```

**1.4 Tạo file project, ví dụ `phan_loai_khieu_nai.py`**

---

### Bước 2 — Build lớp Redaction (Lớp 2)

Đây là lớp đầu tiên và quan trọng nhất cần build. Nhớ: PII phải bị ẩn **trước khi** làm bất cứ điều gì khác.

```python
# phan_loai_khieu_nai.py

import re
import os
import json
import anthropic
from dotenv import load_dotenv

# Load biến môi trường từ file .env
load_dotenv()

# ============================================================
# LỚP 2: TIỀN XỬ LÝ + REDACTION PII
# ============================================================

def redact_pii(text: str) -> str:
    """
    Ẩn thông tin định danh cá nhân (PII) trong text trước khi gửi model.
    
    Xử lý:
    - Số điện thoại VN (10 số, bắt đầu 0) → [SĐT]
    - Địa chỉ email → [EMAIL]
    - Số CCCD/CMND (12 chữ số) → [CCCD]
    
    Giới hạn: Chưa ẩn tên người và địa chỉ — cần NER cho bản production.
    """
    # Ẩn số điện thoại VN: 0xxxxxxxxx (10 số)
    text = re.sub(r"\b0\d{9}\b", "[SĐT]", text)
    
    # Ẩn địa chỉ email
    text = re.sub(r"[\w.+\-]+@[\w\-]+\.[\w.\-]+", "[EMAIL]", text)
    
    # Ẩn số CCCD/CMND (12 chữ số liên tiếp)
    text = re.sub(r"\b\d{12}\b", "[CCCD]", text)
    
    return text


def validate_input(text: str) -> bool:
    """
    Kiểm tra input có hợp lệ không.
    
    Returns:
        True nếu hợp lệ, False nếu không
    """
    if not text or not text.strip():
        return False  # Không được rỗng
    if len(text.strip()) < 10:
        return False  # Quá ngắn
    if len(text) > 2000:
        return False  # Quá dài
    return True
```

**Kiểm tra lớp 2 ngay sau khi viết:**

```python
# Thêm vào cuối file để test
if __name__ == "__main__":
    test_inputs = [
        "Tôi tên Trần Văn Demo, SĐT 0901234567, muốn đổi hàng.",
        "Email demo@example.com, áo bị lỗi chỉ sau một lần giặt.",
        "CCCD 012345678901, giao hàng 5 ngày chưa tới.",
        "Áo bị bung chỉ, không có thông tin cá nhân nào.",
    ]
    
    print("=== Test Redaction ===")
    for text in test_inputs:
        redacted = redact_pii(text)
        print(f"Gốc:   {text}")
        print(f"Sau:   {redacted}")
        print()
```

**Chạy và kiểm tra output:** Phải chắc chắn SĐT, email, CCCD đã được thay bằng placeholder.

---

### Bước 3 — Build lớp Gọi Model (Lớp 3)

Chỉ build lớp này sau khi lớp 2 đã hoạt động đúng.

```python
# Thêm vào phan_loai_khieu_nai.py

# ============================================================
# LỚP 3: GỌI MODEL VỚI STRUCTURED OUTPUT
# ============================================================

# Tập nhãn cho phép — là nguồn của sự thật (source of truth)
# Mọi validation đều so với tập này
TAP_NHAN_HOP_LE = {"doi_tra", "giao_hang", "chat_luong", "khac"}

# System prompt — giới hạn phạm vi của model
SYSTEM_PROMPT = """
Bạn là hệ thống phân loại khiếu nại khách hàng của Yody — thương hiệu thời trang Việt Nam.

NHIỆM VỤ DUY NHẤT của bạn: Đọc nội dung khiếu nại và phân loại vào đúng một trong bốn nhóm:
- doi_tra: Khách muốn đổi hoặc trả sản phẩm, hoàn tiền
- giao_hang: Vấn đề về giao hàng, vận chuyển, thời gian nhận hàng
- chat_luong: Phàn nàn về chất lượng sản phẩm (vải, đường chỉ, màu sắc, kích thước)
- khac: Các nội dung khác không thuộc ba nhóm trên

ĐỊNH DẠNG TRẢ VỀ (bắt buộc, chỉ JSON, không thêm gì khác):
{"nhan": "<một trong bốn nhãn trên>", "do_tin_cay": <số thực từ 0.0 đến 1.0>}

VÍ DỤ:
Input: "Tôi đặt áo khoác [SĐT] nhưng giao sai size, muốn đổi"
Output: {"nhan": "doi_tra", "do_tin_cay": 0.95}

KHÔNG được: trả lời, giải thích, tư vấn, hay làm bất cứ điều gì khác ngoài phân loại.
"""


def goi_model(noi_dung_da_redact: str) -> dict | None:
    """
    Gọi Claude model để phân loại nội dung đã redact.
    
    Args:
        noi_dung_da_redact: Nội dung khiếu nại đã được ẩn PII
    
    Returns:
        dict với 'nhan' và 'do_tin_cay', hoặc None nếu thất bại
    """
    # Đọc API key từ biến môi trường — KHÔNG hardcode
    api_key = os.environ.get("CLAUDE_API_KEY")
    model_name = os.environ.get("MODEL_NAME", "claude-opus-4-5")
    
    if not api_key:
        print("[ERROR] CLAUDE_API_KEY chưa được cấu hình")
        return None
    
    try:
        client = anthropic.Anthropic(api_key=api_key)
        
        response = client.messages.create(
            model=model_name,
            max_tokens=100,  # Chỉ cần output JSON ngắn
            system=SYSTEM_PROMPT,
            messages=[
                {"role": "user", "content": noi_dung_da_redact}
            ]
        )
        
        # Lấy text từ response
        raw_text = response.content[0].text.strip()
        
        # Parse JSON
        return json.loads(raw_text)
        
    except anthropic.APITimeoutError:
        print("[WARN] API timeout")
        return None
    except anthropic.APIError as e:
        print(f"[WARN] API error: {e}")
        return None
    except json.JSONDecodeError as e:
        print(f"[WARN] Response không phải JSON hợp lệ: {e}")
        return None
```

---

### Bước 4 — Build lớp Validate + Fallback (Lớp 4 + 5)

Đây là lớp ghép lại tất cả và xử lý mọi tình huống.

```python
# Thêm vào phan_loai_khieu_nai.py

# ============================================================
# LỚP 4: VALIDATE OUTPUT
# ============================================================

NGUONG_TIN_CAY = 0.6  # Model phải ít nhất 60% chắc chắn

def ket_qua_hop_le(r) -> bool:
    """
    Kiểm tra output từ model có hợp lệ để dùng không.
    
    Điều kiện hợp lệ:
    1. Phải là dict (JSON đã parse thành công)
    2. Phải có trường 'nhan'
    3. Nhãn phải nằm trong tập cho phép
    4. Độ tin cậy phải trên ngưỡng tối thiểu
    """
    if not isinstance(r, dict):
        return False
    
    if "nhan" not in r:
        return False
    
    if r["nhan"] not in TAP_NHAN_HOP_LE:
        return False
    
    if r.get("do_tin_cay", 0) < NGUONG_TIN_CAY:
        return False
    
    return True


# ============================================================
# LỚP 5: OUTPUT + FALLBACK — HÀM CHÍNH
# ============================================================

def phan_loai_khieu_nai(noi_dung_email: str) -> str:
    """
    Hàm chính: phân loại email khiếu nại qua toàn bộ pipeline 5 lớp.
    
    Quy trình:
    1. Validate input (Lớp 1+2 đầu)
    2. Redact PII (Lớp 2)
    3. Gọi model (Lớp 3)
    4. Validate output (Lớp 4)
    5. Trả nhãn hoặc fallback (Lớp 5)
    
    Returns:
        Một trong: 'doi_tra', 'giao_hang', 'chat_luong', 'khac', 'can_nguoi_xu_ly'
    """
    # --- BƯỚC 1: Validate input (Lớp 1+2)
    if not validate_input(noi_dung_email):
        print("[INFO] Input không hợp lệ → fallback")
        return "can_nguoi_xu_ly"
    
    # --- BƯỚC 2: Redact PII (Lớp 2)
    # QUAN TRỌNG: bước này PHẢI chạy trước khi gọi model
    noi_dung_sach = redact_pii(noi_dung_email)
    
    # Log để debug — chú ý: log nội dung ĐÃ redact, không log nội dung gốc
    print(f"[DEBUG] Nội dung sau redact: {noi_dung_sach[:100]}...")
    
    # --- BƯỚC 3: Gọi model (Lớp 3)
    ket_qua = goi_model(noi_dung_sach)
    
    # --- BƯỚC 4 + 5: Validate và Fallback
    if ket_qua is None:
        # Model không phản hồi hoặc có lỗi
        print("[INFO] Không nhận được kết quả từ model → fallback")
        return "can_nguoi_xu_ly"
    
    if not ket_qua_hop_le(ket_qua):
        # Kết quả không hợp lệ hoặc không đủ tin cậy
        print(f"[INFO] Kết quả không đủ điều kiện (kết quả: {ket_qua}) → fallback")
        return "can_nguoi_xu_ly"
    
    # Kết quả tốt — trả nhãn
    nhan = ket_qua["nhan"]
    do_tin_cay = ket_qua["do_tin_cay"]
    print(f"[INFO] Phân loại thành công: {nhan} (độ tin cậy: {do_tin_cay:.0%})")
    return nhan
```

---

### Bước 5 — Chạy thử và kiểm tra từng ca

```python
# Thêm vào cuối phan_loai_khieu_nai.py

# ============================================================
# CHẠY THỬ VỚI DỮ LIỆU MẪU
# ============================================================

if __name__ == "__main__":
    print("=" * 60)
    print("TEST PIPELINE PHÂN LOẠI KHIẾU NẠI")
    print("=" * 60)
    
    test_cases = [
        {
            "mo_ta": "Ca 1: Đổi/trả + có PII (test redaction)",
            "input": "Tôi tên Trần Văn Demo, SĐT 0901234567, "
                     "đặt áo khoác nhưng giao sai size, muốn đổi.",
            "ky_vong": "doi_tra"
        },
        {
            "mo_ta": "Ca 2: Giao hàng + có email (test redaction)",
            "input": "Đơn của tôi 5 ngày chưa giao, "
                     "ai xử lý giúp? Email demo@example.com",
            "ky_vong": "giao_hang"
        },
        {
            "mo_ta": "Ca 3: Chất lượng (không có PII)",
            "input": "Vải áo bị bung chỉ sau một lần giặt, chất lượng kém.",
            "ky_vong": "chat_luong"
        },
        {
            "mo_ta": "Ca 4: Mơ hồ (test fallback)",
            "input": "Tôi muốn vừa đổi hàng vừa hỏi sao giao chậm.",
            "ky_vong": "can_nguoi_xu_ly"  # Có thể là doi_tra hoặc giao_hang
        },
        {
            "mo_ta": "Ca 5: Khác",
            "input": "Sản phẩm tốt, không có gì phàn nàn, cảm ơn shop.",
            "ky_vong": "khac"
        },
    ]
    
    ket_qua_tong = {"pass": 0, "fail": 0}
    
    for i, ca in enumerate(test_cases, 1):
        print(f"\n--- {ca['mo_ta']} ---")
        print(f"Input: {ca['input']}")
        
        ket_qua = phan_loai_khieu_nai(ca['input'])
        
        # Ca 4 là mơ hồ — chấp nhận nhiều nhãn
        if i == 4:
            pass_fail = "✓" if ket_qua in {"doi_tra", "giao_hang", "can_nguoi_xu_ly"} else "✗"
        else:
            pass_fail = "✓" if ket_qua == ca["ky_vong"] else "✗"
        
        print(f"Kết quả: {ket_qua} {pass_fail} (kỳ vọng: {ca['ky_vong']})")
        
        if "✓" in pass_fail:
            ket_qua_tong["pass"] += 1
        else:
            ket_qua_tong["fail"] += 1
    
    print(f"\n{'=' * 60}")
    print(f"KẾT QUẢ: {ket_qua_tong['pass']} pass / {ket_qua_tong['fail']} fail")
    print("=" * 60)
    
    # Quan trọng: kiểm tra log
    print("\n⚠️  Hãy kiểm tra log ở trên:")
    print("1. Ca 1: 'Trần Văn Demo' vẫn xuất hiện (chưa ẩn tên — đây là giới hạn của regex)")
    print("2. Ca 1: '0901234567' phải đã thành '[SĐT]'")
    print("3. Ca 2: 'demo@example.com' phải đã thành '[EMAIL]'")
```

### Những điều cần kiểm tra sau khi chạy thử

Sau khi chạy script, hãy xem lại log và tự hỏi:

**Về redaction:**
- [ ] SĐT đã được thay bằng `[SĐT]` chưa?
- [ ] Email đã được thay bằng `[EMAIL]` chưa?
- [ ] Log không hiển thị PII gốc chứ? (Chúng ta đang log `noi_dung_sach` chứ không phải `noi_dung_email`)

**Về fallback:**
- [ ] Ca mơ hồ (Ca 4) có trigger fallback hoặc cho ra nhãn hợp lý không?
- [ ] Nếu tắt mạng và chạy lại, hàm có trả `can_nguoi_xu_ly` thay vì crash không?

**Về secret:**
- [ ] API key có đang được đọc từ biến môi trường không? (Kiểm tra trong code — không được thấy key hardcoded)

---

## 9. Lab Build — Tự xây feature của bạn (90 phút)

### Đề bài

Lấy **canvas Product Thinking** từ buổi I4.1 của bạn. Chọn **một feature AI** từ canvas đó và build nó thành một script Python chạy được, kèm trust layer tối thiểu.

**Yêu cầu kỹ thuật tối thiểu:**
- **Một luồng rõ ràng:** một input → một AI call có nghĩa → một output demo được
- **Redaction PII:** nếu input có thể chứa thông tin cá nhân, phải có bước redact trước khi gửi model
- **Validate output:** kiểm tra output từ model trước khi dùng
- **Fallback:** ít nhất một đường lui khi AI sai/rỗng/timeout
- **Không hardcode secret:** API key phải đọc từ biến môi trường
- **Có thể giải thích từng hàm trong code:** khi mentor/reviewer hỏi, bạn phải giải thích được từng đoạn đang làm gì — không merge mù

**Không cần:**
- UI đẹp — terminal output là đủ
- Feature phức tạp — một input → một output là đủ
- 100% accuracy — mục tiêu là kiến trúc đúng, không phải kết quả hoàn hảo

### Checklist Trust Layer — Tự rà khi build

In ra và check từng mục khi build xong:

- [ ] **Validate input:** Input được kiểm tra trước khi xử lý (độ dài, định dạng, không rỗng)
- [ ] **Redaction PII:** Tất cả PII (SĐT, email, CCCD) được ẩn **trước khi** gửi model
- [ ] **Structured output:** Model được yêu cầu trả định dạng cố định (JSON) — không phải text tự do
- [ ] **Validate output:** Output từ model được kiểm tra định dạng và giá trị trước khi dùng
- [ ] **Fallback hoạt động:** Test tắt mạng hoặc truyền input mơ hồ — hệ thống không crash
- [ ] **Secret an toàn:** API key được đọc từ biến môi trường, không hardcode trong code
- [ ] **Log sạch:** Kiểm tra log output — không chứa PII gốc (chỉ chứa PII đã redact)

### Tiêu chí ĐẠT

Feature của bạn đạt tiêu chí nếu đáp ứng **cả ba điều kiện** sau:

**1. Chạy được end-to-end**
- Chạy script và truyền input → nhận được output có nghĩa, không crash
- Có ít nhất 2-3 test case chạy được

**2. Có trust layer thật**
- Ít nhất một cơ chế validate input (không phải comment — phải là code chạy được)
- Ít nhất một cơ chế validate output (kiểm tra nhãn, định dạng, hoặc ngưỡng tin cậy)
- Ít nhất một fallback có ý nghĩa khi AI sai/rỗng (không phải `return None`)

**3. Không vi phạm ranh giới cứng**
- Có hàm redact PII và nó được gọi trước khi truyền vào model (nếu input có thể chứa PII)
- API key không hardcode trong code — đọc từ biến môi trường
- `.env` không được commit (có trong `.gitignore`)

### Gợi ý các feature phù hợp để build

Nếu canvas của bạn chưa rõ ràng, đây là một số feature phù hợp với quy mô lab 90 phút:

| Feature | Input | Output | Bối cảnh Yody |
|---------|-------|--------|---------------|
| Phân loại khiếu nại | Email text | Nhãn loại | CSKH routing |
| Gợi ý tag sản phẩm | Mô tả sản phẩm | Danh sách tags | Merchandising |
| Phân loại review | Nội dung review | Positive/Negative/Neutral | Product feedback |
| Tóm tắt feedback | Nhiều comment | Bản tóm tắt ngắn | Team review |
| Phát hiện câu hỏi FAQ | Tin nhắn khách | Có/không phải FAQ | Chatbot pre-routing |

---

## 10. Code Review cặp đôi — Đọc code người khác như thế nào

### Tại sao code review quan trọng?

Code review không phải là tìm lỗi để "bắt bẻ" nhau — đó là một cách học và một lớp bảo vệ thứ hai.

Khi bạn build một feature, bạn biết nó hoạt động như thế nào — bạn đã test các ca bạn nghĩ đến. Người review sẽ nhìn thấy những ca bạn chưa nghĩ đến, những assumption bạn đang làm ngầm, và những edge case bạn đã bỏ qua.

Đặc biệt với AI feature, code review còn giúp kiểm tra trust layer — thứ rất dễ bị bỏ qua khi viết.

### Bốn điểm cần rà khi review AI feature

**1. Trust layer có đủ không?**

Hỏi người viết:
- Input được validate chưa? Validate gì? Ở đâu trong code?
- Output được kiểm tra trước khi dùng chưa? Kiểm tra cụ thể cái gì?
- Nếu model trả về định dạng khác mong đợi, code xử lý thế nào?

Tìm kiếm trong code: Có `if` statement nào kiểm tra output không? Hay code dùng trực tiếp `result["key"]` không có bước kiểm tra trước?

---

**2. PII có được ẩn đúng không?**

Hỏi người viết:
- Input của feature có thể chứa PII không?
- Nếu có, hàm redact được gọi ở đâu?
- Hàm redact có được gọi **trước** khi truyền vào model không?

Tìm kiếm trong code: Tìm hàm gọi model. Nhìn vào input được truyền vào đó. Nó có đi qua bước redact trước không? Hay nó là raw input từ người dùng?

Thử nghiệm: Truyền input có SĐT thật (giả lập). Kiểm tra log — SĐT có bị ẩn không?

---

**3. Fallback có thật không?**

Hỏi người viết:
- Nếu model timeout, code xử lý thế nào?
- Nếu model trả về format khác mong đợi, code xử lý thế nào?
- "Fallback" trong code có thực sự xử lý được hay chỉ là `return None` hoặc `pass`?

Thử nghiệm thực tế:
- Ngắt mạng và chạy — hệ thống crash hay trả fallback?
- Truyền input mơ hồ hoặc ngắn vô nghĩa — hệ thống xử lý thế nào?

---

**4. Secret có an toàn không?**

Kiểm tra trực tiếp trong code:
- Có chuỗi nào trông như API key không? (bắt đầu bằng `sk-`, `Bearer `, dài 40+ ký tự)
- Key được đọc từ `os.environ` hay từ biến hardcoded?
- File `.env` có trong `.gitignore` không?

```bash
# Lệnh kiểm tra nhanh
grep -rn "sk-" .                    # Tìm chuỗi trông như API key
grep -rn "api_key\s*=" .            # Tìm assignment api_key
cat .gitignore | grep ".env"        # Kiểm tra .env có trong gitignore không
```

### Quy trình code review cặp đôi (20 phút)

**Phân chia vai:** Người A review code của Người B, và ngược lại.

**Bước 1 (5 phút):** Người viết chạy demo feature của mình — show end-to-end và ít nhất một ca fallback.

**Bước 2 (10 phút):** Người review đọc code và hỏi về bốn điểm: trust layer, redaction PII, fallback, secret. Người viết giải thích từng phần.

> **Quy tắc quan trọng:** Nếu người viết không giải thích được một đoạn code, đó là dấu hiệu của "vibe coding không kỷ luật" — merge code mà chưa đọc-hiểu. Đây là thứ cần sửa.

**Bước 3 (5 phút):** Thống nhất ít nhất một vấn đề cần sửa. Người viết sửa ngay trong buổi.

### Sau code review: sửa và ghi chú

Mỗi người cần:
1. Sửa ít nhất một vấn đề được chỉ ra
2. Thêm comment giải thích vào những đoạn code phức tạp
3. Ghi lại: trust layer của feature có những cơ chế gì (để mang sang I4.3)

---

## 11. Tóm tắt & bước tiếp theo

### Năm điều cốt lõi từ buổi hôm nay

**1. AI feature có 5 lớp, không chỉ là "gọi model"**

Đầu vào → Tiền xử lý & Redaction → Gọi model → Hậu xử lý & Validate → Đầu ra & Fallback. Bốn trong năm lớp là trust layer. Chỉ một lớp là gọi AI.

**2. Trust layer là phần đáng giá, không phải dòng gọi model**

Ai cũng gọi model được sau 5 phút học API. Điều phân biệt builder level 4 là biết bảo vệ hệ thống, dữ liệu, và người dùng khỏi những lúc AI thất bại.

**3. Vibe coding phải có kỷ luật**

Claude Code sinh code nhanh — đó là sức mạnh. Nhưng bạn phải đọc-hiểu từng thay đổi trước khi giữ. Tốc độ không thay thế trách nhiệm.

**4. Secret và PII là ranh giới cứng**

Không hardcode API key. Luôn redact PII trước khi gửi model. Log không chứa PII. Ba quy tắc này không có ngoại lệ trong bất kỳ feature nào ở Yody.

**5. Fallback tạo nên trải nghiệm khi AI sai**

AI sẽ có lúc sai — đây là thực tế, không phải bug. Feature tốt là feature có đường lui duyên dáng khi điều đó xảy ra.

### Bài tập về nhà (chuẩn bị cho I4.3)

1. **Hoàn thiện feature** từ lab hôm nay — đảm bảo toàn bộ checklist trust layer được check
2. **Gắn feature vào initiative thật** của bạn tại Yody (đã ẩn PII, chạy được với dữ liệu giả lập)
3. **Ghi chú trust layer** — mô tả feature của bạn có cơ chế nào ở mỗi lớp
4. **Cho mentor xem** trước buổi I4.3

### Buổi tiếp theo: I4.3 — Tích hợp Initiative & Iterate (Gate L4→L5)

Buổi I4.3 là buổi **gate** — feature bạn build hôm nay sẽ được đưa vào một initiative thật có stakeholder ngoài team. Bạn sẽ thu feedback và iterate ít nhất 2 vòng để tạo bằng chứng cho Gate L4→L5.

Feature hôm nay là vật liệu cho buổi đó — hãy build cho tốt.

### Tài nguyên học thêm

Nếu bạn muốn đi sâu hơn về các chủ đề trong buổi hôm nay:

- **Claude Code 101** (Anthropic Academy) — Bắt đầu với Claude Code: [anthropic.skilljar.com/claude-code-101](https://anthropic.skilljar.com/claude-code-101)
- **Claude Code in Action** (Anthropic Academy) — Build thật với Claude Code: [anthropic.skilljar.com/claude-code-in-action](https://anthropic.skilljar.com/claude-code-in-action)
- **Building with the Claude API** (Anthropic Academy) — Tích hợp Claude API vào sản phẩm: [anthropic.skilljar.com/claude-with-the-anthropic-api](https://anthropic.skilljar.com/claude-with-the-anthropic-api)

---

## 12. Phụ lục: Dữ liệu mẫu để thực hành

> ⚠️ **Lưu ý:** Tất cả tên, số điện thoại, email, CCCD dưới đây đều là **dữ liệu giả lập hoàn toàn** — chỉ dùng để test. Tuyệt đối không thay thế bằng dữ liệu thật của khách hàng Yody trong bất kỳ buổi lab nào.

### Sáu email khiếu nại mẫu

**Email #1 — Đổi/trả + có PII (test redaction)**
```
Tôi tên Trần Văn Demo, SĐT 0901234567, đặt áo khoác nhưng giao sai size, muốn đổi.
```
*Kỳ vọng:* Nhãn `doi_tra`  
*Test gì:* SĐT `0901234567` phải thành `[SĐT]` trong log

---

**Email #2 — Giao hàng + có email (test redaction)**
```
Đơn của tôi 5 ngày chưa giao, ai xử lý giúp? Email demo@example.com
```
*Kỳ vọng:* Nhãn `giao_hang`  
*Test gì:* `demo@example.com` phải thành `[EMAIL]` trong log

---

**Email #3 — Chất lượng (không có PII)**
```
Vải áo bị bung chỉ sau một lần giặt, chất lượng kém.
```
*Kỳ vọng:* Nhãn `chat_luong`  
*Test gì:* Không có PII để redact — nhưng redact function phải chạy mà không lỗi

---

**Email #4 — Khác + có CCCD (test redaction)**
```
Cho hỏi CCCD 012345678901 của tôi đã được xác minh chưa? Tôi cần làm thủ tục đổi trả.
```
*Kỳ vọng:* Nhãn `khac`  
*Test gì:* `012345678901` phải thành `[CCCD]` trong log

---

**Email #5 — Mơ hồ (test fallback)**
```
Tôi muốn vừa đổi hàng vừa hỏi sao giao chậm.
```
*Kỳ vọng:* Có thể là `doi_tra`, `giao_hang`, hoặc `can_nguoi_xu_ly` — cả ba đều chấp nhận được  
*Test gì:* Model có nhận ra đây là case mơ hồ và phản ánh qua độ tin cậy thấp không?

---

**Email #6 — Không phàn nàn**
```
Sản phẩm tốt, không có gì phàn nàn, cảm ơn shop.
```
*Kỳ vọng:* Nhãn `khac`  
*Test gì:* Model có phân loại đúng nội dung tích cực vào `khac` không?

---

### Tập nhãn cho phép

```python
TAP_NHAN_HOP_LE = {"doi_tra", "giao_hang", "chat_luong", "khac"}
NHAN_FALLBACK = "can_nguoi_xu_ly"
```

### Mẫu cấu hình `.env`

```
# File: .env — KHÔNG commit file này lên git
# Nhận key từ người hướng dẫn trong sandbox

CLAUDE_API_KEY=sk-ant-...         # Điền key được cấp
MODEL_NAME=claude-opus-4-5        # Hoặc model được chỉ định
```

### Checklist cuối buổi (tự kiểm tra trước khi nộp)

- [ ] Feature chạy end-to-end với ít nhất 3 test case
- [ ] Log không hiển thị SĐT/email/CCCD gốc (chỉ hiện placeholder)
- [ ] Tắt mạng và chạy → không crash, trả về `can_nguoi_xu_ly`
- [ ] Tìm kiếm `grep -rn "sk-" .` → không tìm thấy API key hardcoded
- [ ] `.env` có trong `.gitignore`
- [ ] Có thể giải thích từng hàm trong code làm gì (không merge mù)

---

*YODY Product Builder Program — Tài Liệu Học I4.2 · v1.0 · 25/06/2026*  
*Module 4 (Tích hợp & Product Thinking) · L4 Integrator*  
*Brand: #2a2b86 / #fcaf16 / Montserrat*
