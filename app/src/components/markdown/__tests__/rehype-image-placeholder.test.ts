import { describe, it, expect } from "vitest";

/**
 * @deprecated The rehype-side pipeline was replaced — see
 * `./remark-image-placeholder.test.ts` for the active tests.
 */
describe("rehype-image-placeholder (deprecated)", () => {
  it("is deprecated and replaced by remark-image-placeholder", () => {
    expect(true).toBe(true);
  });
});
